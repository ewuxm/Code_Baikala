# Project

## Local setup

1. Install dependencies:

```bash
pip install -r requirements.txt
```

2. Copy `.env.example` to `.env`.
3. Fill `.env` with your own secrets.
4. Run:

```bash
python app.py
```

`.env` is intentionally excluded from Git.
